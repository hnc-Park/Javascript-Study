// counter.js 의 increase 를 가져옴
// import {increase} from './counter.js';

// counter.js 의 increase 를 increase1 이름으로 가져옴
// import {increase as increase1 } from './counter.js';

// counter.js 의 increase, getCount 를 가져옴
import { increase, getCount } from './counter.js';

// counter.js 모듈의 모든 export 데이터를 counter 로 묶어 가져옴
// import * as counter from './counter.js';

// counter.increase();
// counter.increase();
// counter.increase();
// console.log(counter.getCount());

increase();
increase();
increase();
console.log(getCount());
